from dotenv import load_dotenv
from apresentacao.tela_principal import iniciar_tela_principal

# Carrega as variaveis de ambiente do arquivo .env local
load_dotenv()

if __name__ == "__main__":
    iniciar_tela_principal()
